* Project

** Milestone 1

- create a new schedule
- add events to schedule
- edit event
- remove event
- persist schedule
- output csv of schedule to upload to google sheets
- events
  - series
    - event page
    - results page
    - race
      - start date
      - dock time
      - start time
      - end date
      - time limit

*** decisions

- programming language
  Go or Rust
  - go
- persistent store
  postgresql
- data modeling

- bazel for building
- install dependencies or use containers
  - containers
  - rancher desktop

*** todo

- [X] set up bazel for golang
  set up bazel files and configured with gazell
- [ ] create cli
  - [ ] connect model and cli
    This is now client server so need to make cli to connect to server and server needs to be running
- [-] persistence
  following this example https://github.com/oshalygin/gqlgen-pg-todo-example/blob/master/main.go
  - [X] install postgres
    homebrew
  - [ ] configure server.go to use postgres backend
  - [ ] persistence backend for gqlgen
- [-] data model
  - [X] try gqlgen
  - [X] schedule
    - create
    - query
  - [-] top down traversal from schedule to races
    - [X] schedule to series
    - [ ] series to races
  - [ ] event interface (maybe)
  - [ ] series with one or more races
    - [ ] race
  - [ ] create
  - [ ] add crew to event by date
  - [ ] generate calendar in isc format
- [ ] commands
  - [ ] command to add schedule
  - [ ] command to add event to schedule
  - [ ] command to output schedule along with all events


* data model

** schedule

- name
- events

** events - interface? - not sure I need this yet
- name
- type: series, race, delivery, maint, social

** link
- name
- link

** series
- name
- races[]
- links[]

** race
- name
- start date
- start time


** schedule

*** CLI

#+begin_example
crewd schedule new NAME
#+end_example

*** Schedule

#+name: schedule-model
#+begin_src d2
schedule {
  name
  events
}
#+end_src

* issues

** ent

*** running go generate

- ~bazel run //:gazelle~ was able to create build files to run generate

- not happy with ent need to look into a different persistence / updating / quering package
  - maybe gqlgen https://gqlgen.com, https://github.com/99designs/gqlgen
  - finish current integration of persistence and command then decide
